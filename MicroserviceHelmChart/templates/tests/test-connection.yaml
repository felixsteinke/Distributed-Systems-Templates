apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "MicroserviceHelmChart.fullname" . }}-test-connection"
  labels:
    {{- include "MicroserviceHelmChart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "MicroserviceHelmChart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
